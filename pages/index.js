import React from 'react';
import Head from 'next/head';
import { Button } from 'reactstrap';

const Home = () => (
  <div>
    <Head>
      <title>Home</title>
    </Head>

    <Button>Button</Button>
  </div>
);

export default Home;
